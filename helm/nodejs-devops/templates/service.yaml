apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-devops.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "nodejs-devops.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP